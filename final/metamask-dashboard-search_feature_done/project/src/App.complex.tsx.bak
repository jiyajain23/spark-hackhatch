import React, { useState, useEffect } from "react";
import {
  Coins,
  Trophy,
  Users,
  Video,
  Wallet,
  Calendar,
  TrendingUp,
  LineChart,
  Link as LinkIcon,
  Search,
} from "lucide-react";
import {
  LineChart as RechartsLineChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
} from "recharts";
import WalletConnect from "./WalletConnect";
import YouTuberSearch from "./YouTuberSearch";
import YouTuberDashboard from "./YouTuberDashboard";
import { Link } from "react-router-dom";
import { YouTuber } from "./types";
import { LampDemo } from "./components/ui/lamp";
import { SplineSceneBasic } from "./components/ui/spline-demo";

function App() {
  const [searchQuery, setSearchQuery] = useState("");
  const [connectedAccount, setConnectedAccount] = useState<string | null>(null);
  const [ethersProvider, setEthersProvider] = useState<any>(null);
  const [selectedYouTuber, setSelectedYouTuber] = useState<YouTuber | null>(
    null
  );
  const [showMobileSearch, setShowMobileSearch] = useState(false);

  const handleWalletConnect = (account: string, provider: any) => {
    setConnectedAccount(account);
    setEthersProvider(provider);
    // Only log in development
    if (import.meta.env.DEV) {
    console.log("Connected to wallet:", account);
    }
  };

  const handleYouTuberSelect = (youtuber: YouTuber) => {
    setSelectedYouTuber(youtuber);
    window.scrollTo(0, 0); // Scroll to top when selecting a YouTuber
  };

  const handleBackToSearch = () => {
    setSelectedYouTuber(null);
  };

  const performanceData = [
    { name: "Jan", value: 4000 },
    { name: "Feb", value: 3000 },
    { name: "Mar", value: 5000 },
    { name: "Apr", value: 4800 },
    { name: "May", value: 6000 },
    { name: "Jun", value: 7000 },
  ];

  const upcomingEvents = [
    {
      id: 1,
      title: "Creator Summit 2025",
      date: "March 15, 2025",
      description:
        "Join top creators for an exclusive virtual event discussing the future of content creation.",
    },
    {
      id: 2,
      title: "Token Launch: Tech Reviews Pro",
      date: "March 20, 2025",
      description:
        "Be part of the initial token offering for one of tech's most influential reviewers.",
    },
    {
      id: 3,
      title: "Community Meetup",
      date: "April 1, 2025",
      description:
        "Virtual meetup with token holders - exclusive Q&A and behind-the-scenes content.",
    },
  ];

  return (
    <div className="min-h-screen bg-background">
      {/* Navigation */}
      <nav className="sticky top-0 z-50 border-b border-nav-border bg-nav-bg backdrop-blur-md shadow-sm">
        <div className="container mx-auto px-4 py-4 flex items-center justify-between gap-4">
          {/* Logo Section */}
          <Link
            to="/"
            className="flex items-center space-x-2 group transition-all duration-300"
          >
            <div className="p-2 bg-primary rounded-lg group-hover:scale-110 transition-all duration-300 group-hover:shadow-[0_0_20px_rgba(59,130,246,0.4)]">
              <LinkIcon className="h-5 w-5 text-primary-foreground" />
          </div>
            <span className="text-xl font-bold group-hover:text-primary transition-colors duration-300">
              CreatorTokens
            </span>
          </Link>

          {/* Search Bar - Centered */}
          <div className="flex-1 max-w-xl mx-4 hidden md:block">
            <YouTuberSearch
              searchQuery={searchQuery}
              setSearchQuery={setSearchQuery}
              onYouTuberSelect={handleYouTuberSelect}
            />
          </div>
          
          {/* Mobile Search Toggle */}
          <button
            onClick={() => setShowMobileSearch(!showMobileSearch)}
            className="md:hidden p-2 rounded-lg hover:bg-primary/10 hover:text-primary transition-all duration-300"
            aria-label="Toggle search"
          >
            <Search className="h-5 w-5" />
          </button>
          
          {/* Mobile Search Bar */}
          {showMobileSearch && (
            <div className="absolute top-full left-0 right-0 p-4 bg-nav-bg border-b border-nav-border md:hidden z-40">
              <YouTuberSearch
                searchQuery={searchQuery}
                setSearchQuery={setSearchQuery}
                onYouTuberSelect={(youtuber) => {
                  handleYouTuberSelect(youtuber);
                  setShowMobileSearch(false);
                }}
              />
            </div>
          )}

          {/* Right Side - Wallet & Portal */}
          <div className="flex items-center space-x-3">
            <WalletConnect onConnect={handleWalletConnect} />
          <Link
            to="/login"
              className="px-4 py-2 rounded-lg text-foreground hover:bg-primary/10 hover:text-primary transition-all duration-300 hover:scale-105"
          >
            Creator Portal
          </Link>
          </div>
        </div>
      </nav>

      {/* Conditionally render either the YouTuberDashboard or the rest of the app */}
      {selectedYouTuber ? (
        <YouTuberDashboard
          youtuber={selectedYouTuber}
          onBack={handleBackToSearch}
        />
      ) : (
        <>
          {/* Hero Section with Lamp Effect */}
          <section className="relative bg-gradient-to-b from-sky-50 via-blue-50 to-sky-100">
            <LampDemo />
            <div className="absolute bottom-12 md:bottom-16 left-1/2 transform -translate-x-1/2 z-50 flex flex-wrap justify-center gap-4 px-4">
              <button className="shimmer-effect px-6 py-3 rounded-lg bg-primary text-primary-foreground hover:bg-primary-hover hover:scale-105 hover:shadow-[0_0_30px_rgba(59,130,246,0.5)] transition-all duration-300 font-medium shadow-lg shadow-[0_0_20px_rgba(59,130,246,0.3)]">
                  Explore Creators
                </button>
              <button className="px-6 py-3 rounded-lg bg-white text-primary border-2 border-primary/30 hover:border-primary hover:bg-primary/5 hover:scale-105 hover:shadow-[0_0_20px_rgba(59,130,246,0.3)] transition-all duration-300 font-medium shadow-md">
                  Learn More
                </button>
              </div>
          </section>

          {/* 3D Interactive Section */}
          <section className="py-20 px-4 bg-gradient-to-b from-sky-100 to-blue-50">
            <div className="container mx-auto">
              <SplineSceneBasic />
            </div>
          </section>

          {/* Investment Returns Section */}
          <section className="py-20 bg-gradient-to-b from-blue-50 to-sky-50">
            <div className="container mx-auto px-4">
              <h2 className="text-3xl font-bold text-center mb-12">
                Investment Performance
              </h2>
              <div className="bg-background rounded-lg p-6 shadow-lg">
                <div className="h-[400px]">
                  <ResponsiveContainer width="100%" height="100%">
                    <RechartsLineChart data={performanceData}>
                      <CartesianGrid strokeDasharray="3 3" />
                      <XAxis dataKey="name" />
                      <YAxis />
                      <Tooltip />
                      <Line
                        type="monotone"
                        dataKey="value"
                        stroke="hsl(var(--primary))"
                        strokeWidth={2}
                      />
                    </RechartsLineChart>
                  </ResponsiveContainer>
                </div>
                <div className="grid grid-cols-1 md:grid-cols-3 gap-8 mt-8">
                  <div className="p-6 bg-secondary rounded-lg">
                    <h3 className="text-lg font-semibold mb-2">Average ROI</h3>
                    <p className="text-3xl font-bold text-primary">+24.5%</p>
                    <p className="text-sm text-muted-foreground">
                      Past 6 months
                    </p>
                  </div>
                  <div className="p-6 bg-secondary rounded-lg">
                    <h3 className="text-lg font-semibold mb-2">
                      Total Value Locked
                    </h3>
                    <p className="text-3xl font-bold text-primary">$2.4M</p>
                    <p className="text-sm text-muted-foreground">
                      Across all creators
                    </p>
                  </div>
                  <div className="p-6 bg-secondary rounded-lg">
                    <h3 className="text-lg font-semibold mb-2">
                      Active Holders
                    </h3>
                    <p className="text-3xl font-bold text-primary">12.5K</p>
                    <p className="text-sm text-muted-foreground">
                      Growing monthly
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </section>

          {/* Upcoming Events Section */}
          <section className="py-20">
            <div className="container mx-auto px-4">
              <h2 className="text-3xl font-bold text-center mb-12">
                Upcoming Events
              </h2>
              <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
                {upcomingEvents.map((event) => (
                  <div
                    key={event.id}
                    className="bg-background rounded-lg p-6 shadow-lg border"
                  >
                    <div className="flex items-start mb-4">
                      <Calendar className="h-6 w-6 text-primary mr-3" />
                      <div>
                        <h3 className="text-xl font-semibold">{event.title}</h3>
                        <p className="text-muted-foreground">{event.date}</p>
                      </div>
                    </div>
                    <p className="text-muted-foreground">{event.description}</p>
                    <button className="mt-4 w-full px-4 py-2 rounded-lg bg-secondary text-secondary-foreground hover:bg-secondary/90">
                      Learn More
                    </button>
                  </div>
                ))}
              </div>
            </div>
          </section>

          {/* Features Grid */}
          <section className="py-20 bg-gradient-to-b from-sky-50 to-blue-50">
            <div className="container mx-auto px-4">
              <h2 className="text-3xl font-bold text-center mb-12">
                Platform Features
              </h2>
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <FeatureCard
                  icon={<Wallet className="h-8 w-8" />}
                  title="Token Purchase & Resale"
                  description="Buy and sell creator tokens with traditional payment methods or crypto wallets."
                />
                <FeatureCard
                  icon={<Video className="h-8 w-8" />}
                  title="Exclusive Content"
                  description="Access behind-the-scenes footage and early content releases."
                />
                <FeatureCard
                  icon={<Users className="h-8 w-8" />}
                  title="Community Access"
                  description="Join private communities and interact directly with creators."
                />
                <FeatureCard
                  icon={<Trophy className="h-8 w-8" />}
                  title="Rewards & Benefits"
                  description="Earn rewards and unlock special perks based on token holdings."
                />
              </div>
            </div>
          </section>

          {/* Featured Creators */}
          <section className="py-20">
            <div className="container mx-auto px-4">
              <h2 className="text-3xl font-bold text-center mb-12">
                Featured Creators
              </h2>
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <CreatorCard
                  image="/api/placeholder/400/320"
                  name="Alex Thompson"
                  category="Tech Reviews"
                  tokenPrice="$5.99"
                  holders={1234}
                  performance="+12.5%"
                />
                <CreatorCard
                  image="/api/placeholder/400/320"
                  name="Sarah Chen"
                  category="Lifestyle & Fashion"
                  tokenPrice="$4.99"
                  holders={2156}
                  performance="+8.3%"
                />
                <CreatorCard
                  image="/api/placeholder/400/320"
                  name="Marcus Rodriguez"
                  category="Gaming"
                  tokenPrice="$3.99"
                  holders={3789}
                  performance="+15.7%"
                />
              </div>
            </div>
          </section>

          {/* Footer */}
          <footer className="bg-gradient-to-b from-blue-100 to-blue-200 py-12 border-t border-blue-300">
            <div className="container mx-auto px-4">
              <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
                <div>
                  <div className="flex items-center space-x-2 mb-4">
                    <Coins className="h-6 w-6" />
                    <span className="text-xl font-bold">CreatorTokens</span>
                  </div>
                  <p className="text-muted-foreground">
                    Revolutionizing creator-fan relationships through
                    tokenization.
                  </p>
                </div>
                <div>
                  <h3 className="font-semibold mb-4">Platform</h3>
                  <ul className="space-y-2">
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        How it Works
                      </a>
                    </li>
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        Pricing
                      </a>
                    </li>
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        FAQ
                      </a>
                    </li>
                  </ul>
                </div>
                <div>
                  <h3 className="font-semibold mb-4">Company</h3>
                  <ul className="space-y-2">
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        About
                      </a>
                    </li>
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        Careers
                      </a>
                    </li>
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        Contact
                      </a>
                    </li>
                  </ul>
                </div>
                <div>
                  <h3 className="font-semibold mb-4">Legal</h3>
                  <ul className="space-y-2">
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        Privacy Policy
                      </a>
                    </li>
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        Terms of Service
                      </a>
                    </li>
                    <li>
                      <a
                        href="#"
                        className="text-muted-foreground hover:text-foreground"
                      >
                        Cookie Policy
                      </a>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
          </footer>
        </>
      )}
    </div>
  );
}

interface FeatureCardProps {
  icon: React.ReactNode;
  title: string;
  description: string;
}

function FeatureCard({ icon, title, description }: FeatureCardProps) {
  return (
    <div className="bg-background rounded-lg p-6 shadow-lg">
      <div className="mb-4 text-primary">{icon}</div>
      <h3 className="text-xl font-semibold mb-2">{title}</h3>
      <p className="text-muted-foreground">{description}</p>
    </div>
  );
}

interface CreatorCardProps {
  image: string;
  name: string;
  category: string;
  tokenPrice: string;
  holders: number;
  performance: string;
}

function CreatorCard({
  image,
  name,
  category,
  tokenPrice,
  holders,
  performance,
}: CreatorCardProps) {
  return (
    <div className="bg-background rounded-lg overflow-hidden shadow-lg">
      <img src={image} alt={name} className="w-full h-48 object-cover" />
      <div className="p-6">
        <h3 className="text-xl font-semibold mb-2">{name}</h3>
        <p className="text-muted-foreground mb-4">{category}</p>
        <div className="grid grid-cols-3 gap-4 mb-4">
          <div>
            <p className="text-sm text-muted-foreground">Token Price</p>
            <p className="font-semibold">{tokenPrice}</p>
          </div>
          <div>
            <p className="text-sm text-muted-foreground">Holders</p>
            <p className="font-semibold">{holders}</p>
          </div>
          <div>
            <p className="text-sm text-muted-foreground">30d Return</p>
            <p className="font-semibold text-green-600">{performance}</p>
          </div>
        </div>
        <button
          className="w-full mt-4 px-4 py-2 rounded-lg bg-primary text-primary-foreground hover:bg-primary/90"
          onClick={() => {
            if (!connectedAccount) {
              alert("Please connect your wallet first!");
            } else {
              alert("Token purchase feature would be implemented here!");
            }
          }}
        >
          Buy Tokens
        </button>
      </div>
    </div>
  );
}

export default App;
